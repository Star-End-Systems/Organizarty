# Organizarty UI

## Running

- Install npm dependencies 
    ```sh
    npm i
    ```

- Enable tailwind classes hot reload
    ```sh
    npx watch 'npm run buildcss:dev' Pages
    ```

- Run Application
    ```sh
    dotnet watch run
    ```