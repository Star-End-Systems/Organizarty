#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../tree-sitter-cli/cli.js" "$@"
else 
  exec node  "$basedir/../tree-sitter-cli/cli.js" "$@"
fi
